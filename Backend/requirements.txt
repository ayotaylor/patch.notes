# GPU Embedding Service Dependencies
# Compatible with both CUDA (RTX 4070Ti) and MPS (MacBook M2)

torch>=2.0.0
sentence-transformers>=2.2.0
fastapi>=0.100.0
uvicorn[standard]>=0.20.0
pydantic>=2.0.0
numpy>=1.24.0

# Optional: For better logging and monitoring
psutil>=5.9.0